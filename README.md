# DSH 内测群每日档案

本仓库用于私有归档 DeepSeek Harness（DSH）内测期间的群聊信号、成员参与、产品纪事与 Issue 反馈。每个日期对应一份可独立打开的 HTML；README 提供项目说明、每日基本纪要和文件更新记录。

> **仅限获得授权的内部成员查看。** 文件包含群成员昵称和精选发言，请勿转发到公开仓库、公开网盘或社交平台。不得开启无访问门禁的 GitHub Pages。

## 项目介绍

### 档案包含什么

- **今日新增**：对比前一日归档，展示新增 Issue、对话主题和精选对话；
- **信号与纪事**：从有效群消息中收录可行动的问题、建议、实测与明确完成态事件；
- **成员星卡**：只依据群内已验证发言归纳参与情况，不推断真实身份；
- **Issue 汇总**：同步私有仓库 [`dsh-external/issues`](https://github.com/dsh-external/issues) 的 Issue，并按既有规则归类。

### 数据来源与边界

- 群聊只来自本机微信 4.x 数据库中的 `【官方】DSH内测群`；
- Issue 只来自私有仓库 `dsh-external/issues`；
- 页面不包含完整聊天记录、微信数据库、解密密钥、GitHub 凭据、本机配置或本机绝对路径；
- 纪事只收录产品方完成态更新或来源明确的公告，成员疑问、转述与猜测不作为版本更新；
- 所有统计均为截至该文件生成时的累计快照；“今日新增”才表示相较前一份归档的增量。

### 如何查看

- 外发网站：访问 Cloudflare Pages 地址后，使用 GitHub 登录；只有 `dsh-external` 组织成员可以进入；
- GitHub 仓库：获得仓库权限的成员仍可下载对应的 `.html` 文件，在现代浏览器中本地打开；
- HTML 已内嵌样式、交互脚本和当日快照，不依赖同目录的 JSON 或 JavaScript 文件。

## 受保护网站

- 部署源是本私有仓库，但 Cloudflare Pages 只发布构建产生的 `dist/`，不会发布 README、构建脚本或仓库根目录；
- Cloudflare Access 位于所有网页和静态资源之前，使用 GitHub 作为身份提供方，并将访问范围限制为 `dsh-external` 组织；
- 站点响应禁止搜索引擎索引、禁止第三方页面嵌入，并关闭浏览器与边缘缓存；
- `npm run check` 先校验档案完整性和常见敏感路径/凭据，`npm run build` 再生成可部署目录；
- `latest.txt` 明确指定首页对应的当日档案，历史档案发布在受同一权限门禁保护的 `/archive/` 下。

## 每日基本纪要

### 2026-08-05

档案：[DSH 内测群每日档案 · 0805](./DSH内测群每日档案-0805.html)

数据截止：2026-08-05 04:30（北京时间）

| 群消息 | 精选信号 | 成员 | Issue | 纪事 |
| ---: | ---: | ---: | ---: | ---: |
| 8,650 | 120 | 128 | 309 | 6 |

**相较 0804**

- 新增有效群消息 **2,885** 条，新增精选对话 **31** 条；
- 新增成员 **19** 位，新增 Issue **92** 条，新增纪事 **2** 条；
- 新增 Issue 以 **Bug 修复 68 条（73.9%）** 为主，其次是 **功能建议 20 条（21.7%）**；另有其他 3 条、性能优化 1 条。

**讨论重点**

- 插件、Skill 与 MCP 扩展是增量最高的主题（+161）；
- Issue 提报与协作继续升温（+59）；
- 模型选择、调用与额度仍是高频议题（+45）。

**文件更新**

- 新增 `DSH内测群每日档案-0805.html`；
- 更新 `README.md`：补充项目介绍、每日基本纪要与文件更新记录；
- 增加“今日新增”三段页内切换、统计直达、类别占比、重点内容与代表案例；
- 收紧纪事规则，移除成员疑问和转述造成的“内测版本更新”误判；
- 增加本机绝对路径脱敏，并在提交前执行隐私与远端哈希校验。

### 2026-08-04

档案：[DSH 内测群每日档案 · 0804](./DSH内测群每日档案-0804.html)

数据截止：2026-08-04 16:25（北京时间）

| 群消息 | 精选信号 | 成员 | Issue | 纪事 |
| ---: | ---: | ---: | ---: | ---: |
| 5,765 | 120 | 109 | 217 | 4 |

**基本纪要**

- 建立首份可连续比较的群聊与 Issue 基线；
- 累计讨论主要集中在 Issue 反馈、模型使用、插件生态、会话上下文和 Agent 任务；
- 当日尚无前一日归档，因此不提供日增量判断。

**文件更新**

- 新增 `DSH内测群每日档案-0804.html`，作为后续每日对比的基线文件。

## 自动更新方式

- 每天北京时间 05:00 从授权数据源生成当天 `DSH内测群每日档案-MMDD.html`；
- 校验通过后，同步更新当天 HTML、`latest.txt` 与 README 的每日纪要、讨论重点和文件更新记录；
- 只在专用仓库工作区干净且可快进同步时，直接提交并推送到 `main`；
- 任一数据、结构、隐私或 Git 校验失败即停止，不强推、不覆盖仓库中的其他改动。

## 文件命名与保留

每日文件使用 `DSH内测群每日档案-MMDD.html`，例如 8 月 5 日为 `DSH内测群每日档案-0805.html`。历史日期分别保留，不覆盖旧档案。

Private 仓库只能限制 GitHub 访问权限，不能阻止已获权限的人下载或继续转发文件。添加协作者前，请确认对方确实需要查看内测群内容。
